---
import ContentLayout from '../../../layouts/ContentLayout.astro'
import { BASIC_USAGE_SIDEBAR } from '../../../scripts/sidebars'
import { getEntry } from 'astro:content'

const entry = await getEntry('docs', '02-basic-usage/cherrystudio/advanced')
let Content: any = null
let hasContent = false
try {
  if (entry) {
    const rendered = await entry.render()
    Content = rendered.Content
    hasContent = true
  }
} catch (e) {
  console.error('Render error: basic-usage/cherrystudio/advanced', e)
}
---

<ContentLayout
  entry={entry}
  title={entry?.data?.title ?? 'Cherry Studio 进阶'}
  sidebarItems={BASIC_USAGE_SIDEBAR}
  contributors={entry?.data?.contributors}
>
  {hasContent && Content ? <Content /> : <div>文档未找到或渲染失败</div>}
</ContentLayout>
