---
import ContentLayout from '../../layouts/ContentLayout.astro'
import { RESOURCES_SIDEBAR } from '../../scripts/sidebars'

// 添加相对路径信息
const editPath = `src/pages/resources/index.astro`

const headings = [{ id: 'resources', text: '资源合集', depth: 1 }]
---

<ContentLayout
  title="资源合集"
  section="资源合集"
  sidebarItems={RESOURCES_SIDEBAR}
  headings={headings}
  editPath={editPath}
>
  <h1 id="resources">资源合集</h1>
  <p>这里收集了AI相关的优质资源和工具。</p>
</ContentLayout>
