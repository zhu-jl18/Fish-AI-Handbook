---
import ContentLayout from '../../layouts/ContentLayout.astro'
import { FUN_SIDEBAR } from '../../scripts/sidebars'

// 添加相对路径信息
const editPath = `src/pages/fun/index.astro`

const headings = [{ id: 'intro', text: '好玩的', depth: 1 }]
---

<ContentLayout
  title="好玩的"
  section="好玩的"
  sidebarItems={FUN_SIDEBAR}
  headings={headings}
  editPath={editPath}
>
  <h1 id="intro">好玩的</h1>
  <p>这里是一些有趣的AI应用和玩法。</p>
</ContentLayout>
