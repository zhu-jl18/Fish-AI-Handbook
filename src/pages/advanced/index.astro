---
import ContentLayout from "../../layouts/ContentLayout.astro";
import { getEntry } from "astro:content";

let entry = await getEntry("docs", "04-advanced-techniques");
if (!entry) {
  entry = await getEntry("docs", "04-advanced-techniques/index");
}
const { Content } = await entry!.render();

import { ADVANCED_TECHNIQUES_SIDEBAR } from "../../scripts/sidebars";

const headings = [
  { id: "为什么读这一部分", text: "为什么读这一部分", depth: 1 },
];
---

<ContentLayout
  title="工程化进阶：RAG、向量与多智能体"
  section="进阶玩法"
  sidebarItems={ADVANCED_TECHNIQUES_SIDEBAR}
  headings={headings}
>
  <Content />
</ContentLayout>
