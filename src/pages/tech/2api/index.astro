---
import ContentLayout from "../../../layouts/ContentLayout.astro"
import { getEntry } from "astro:content"

const entry = await getEntry("docs", "07-technical-deep-dive/2api")
const { Content } = await entry.render()
---

<ContentLayout title={entry?.data?.title ?? "2API"} section="技术向" headings={[]}>
  <Content />
</ContentLayout>
