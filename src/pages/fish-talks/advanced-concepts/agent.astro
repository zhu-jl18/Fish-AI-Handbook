<![CDATA[---
import ContentLayout from '../../../layouts/ContentLayout.astro';
import { FISH_TALKS_SIDEBAR } from '../../../scripts/sidebars';
import { getEntry } from 'astro:content';

const entry = await getEntry('docs', '01-fish-talks/advanced-concepts/agent');
const { Content } = await entry.render();

const headings = [
  { id: '为什么重要', text: '为什么重要', depth: 1 }
];
---

<ContentLayout 
  title={`Fish想说 - 进阶概念 / Agent（智能体）`}
  section="Fish想说"
  sidebarItems={FISH_TALKS_SIDEBAR}
  headings={headings}
>
  <Content />
</ContentLayout>
]]>