---
title: CCR - GLM 配置
description: 使用 Claude Code Router 接入 GLM 模型的示例配置与注意事项。
tab:
  label: GLM
  order: 2
---

The default call way via anthropic api format does not support `thinking`，so we need to use the way via openai api format with the transformer `force_reasoning`.


Here is more about CLAUDE.md and other custom settings for GLM.


